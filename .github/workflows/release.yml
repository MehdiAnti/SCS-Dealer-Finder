name: Build and Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller colorama

      - name: Build application
        shell: pwsh
        run: |
          pyinstaller `
            --noconfirm `
            --clean `
            --onedir `
            --name "SCS-Dealer-Finder" `
            main.py

      - name: Copy data files
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force "dist/SCS-Dealer-Finder/data"
          Copy-Item "data/converter_pix.exe" "dist/SCS-Dealer-Finder/data/"
          Copy-Item "data/scs_files.txt" "dist/SCS-Dealer-Finder/data/"

      - name: Create ZIP
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          Compress-Archive `
            -Path "dist/SCS-Dealer-Finder" `
            -DestinationPath "SCS-Dealer-Finder-$tag-Windows-x64.zip"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: SCS-Dealer-Finder-Windows-x64
          path: "SCS-Dealer-Finder-${{ github.ref_name }}-Windows-x64.zip"

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ github.ref_name }}" `
            "SCS-Dealer-Finder-${{ github.ref_name }}-Windows-x64.zip" `
            --title "SCS Dealer Finder ${{ github.ref_name }}" `
            --generate-notes
